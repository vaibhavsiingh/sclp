bool func;

void main() 
{
	int func; // A local variable by the same name as that 
                  // of a global can be declared in a local 
                  // scope as their scopes are different
}